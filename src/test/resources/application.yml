spring:
  profiles.active: test
  #datasource:
  #  url: jdbc:h2:tcp://localhost/./data-access-test
  #  username: sa
  #  password:
logging:
  level:
    org.springframework.jdbc: debug
    org.hibernate:
      SQL: debug
      type.descriptor.sql.BasicBinder: trace
